defaultNamespace: cert-manager

helm:
  # Corresponds to: jetstack/cert-manager
  chart: cert-manager
  repo: https://charts.jetstack.io
  
  # Corresponds to: --version v1.19.1
  version: v1.19.1
  
  # Corresponds to: --set installCRDs=true
  values:
    installCRDs: true
